a = float(input('a= '))
b = float(input('b= '))
if a <= b:
    print('\033[32mВідношення справджується! {} дійсно менше {}'.format(a, b))
    # \033[36m -- фарбує текст, що виводиться, у зелений.
    # "{}" та ".format" полегшує виведення значень змінних прямо в тексті.
else:
    a, b = b, a
    print('\033[36mЗначення змінних впорядковано {} <= {}'.format(a, b))
    # \033[36m -- фарбує текст, що виводиться, у блакитний.
non_stop = input()
