def fmax():
    # Записуємо код у функцію(модуль).
    # Це дозволить виконувати код кожного разу, коли напишемо "fmax()"
    a = float(input('a= '))
    b = float(input('b= '))
    if a > b:
        print(' а=', a, ' максимальне')
    elif a == b:
        print(' а рівне b')
    else:
        print(' b=', b, ' максимальне')


fmax()  # Виконує код, записаний у тотожно йменованому модулі.
non_stop = input()
